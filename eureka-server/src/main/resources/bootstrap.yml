eureka:
  client:
    register-with-eureka: false
    fetch-registry: false


spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: ${SPRING_CLOUD_CONFIG_URI:http://localhost:8888} # Spring default behavior, just to be explicit
      fail-fast: true

