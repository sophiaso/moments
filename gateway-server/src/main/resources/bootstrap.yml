#spring:
#  application:
#    name: gateway-server
#  cloud:
#    config:
#      uri: http://localhost:8888
#      fail-fast: true

eureka:
  client:
    register-with-eureka: true

spring:
  resources:
    static-locations: "classpath:/static/dist/"
  cloud:
    gateway:
      routes:
      - id: websocket
        order: 9000
        uri: ws://message-service
        predicates:
        - Path=/message/moments-message
#      - id: message-service
#        uri: lb://message-service
#        predicates:
#        - Path=/message/**

